name: Setup .NET
description: Installs the .NET SDK version used by all workflows.

inputs:
  dotnet-version:
    description: ".NET SDK version"
    default: "8.0.x"

runs:
  using: composite
  steps:
    - name: Setup .NET
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: ${{ inputs.dotnet-version }}

    - name: .NET info
      run: dotnet --info
      shell: bash
